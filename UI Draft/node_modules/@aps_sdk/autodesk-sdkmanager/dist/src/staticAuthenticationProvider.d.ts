import { IAuthenticationProvider } from '@aps_sdk/autodesk-sdkmanager/dist/src/iAuthenticationProvider';
export declare class StaticAuthenticationProvider implements IAuthenticationProvider {
    private _accessToken;
    constructor(accessToken: string);
    getAccessToken(scopes?: string[]): Promise<string>;
}
