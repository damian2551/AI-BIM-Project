import { ApsConfiguration as IApsConfiguration } from '@aps_sdk/autodesk-sdkmanager/dist/src/iApsConfiguration';
import { AuthClient as IAuthClient } from '@aps_sdk/autodesk-sdkmanager/dist/src/iAuthClient';
import { Builder as IBuilder } from '@aps_sdk/autodesk-sdkmanager/dist/src/iBuilder';
import { ILogger } from '@aps_sdk/autodesk-sdkmanager/dist/src/iLogger';
import { IResiliencyConfiguration } from '@aps_sdk/autodesk-sdkmanager/dist/src/iResiliencyConfiguration';
import { SdkManager } from '@aps_sdk/autodesk-sdkmanager/dist/src/sdkManager';
export declare class SdkManagerBuilder implements IBuilder {
    private resiliencyConfiguration;
    private authClient;
    private apsConfiguration;
    private logger;
    static create(): SdkManagerBuilder;
    addResiliencyConfiguration(resiliencyConfiguration: IResiliencyConfiguration): this;
    addAuthClient(authClient: IAuthClient): this;
    addApsConfiguration(apsConfiguration: IApsConfiguration): this;
    addLogger(logger: ILogger): this;
    build(): SdkManager;
}
