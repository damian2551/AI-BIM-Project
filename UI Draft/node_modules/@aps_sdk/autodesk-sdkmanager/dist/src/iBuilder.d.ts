import { IResiliencyConfiguration } from '@aps_sdk/autodesk-sdkmanager/dist/src/iResiliencyConfiguration';
import { AuthClient as IAuthClient } from '@aps_sdk/autodesk-sdkmanager/dist/src/iAuthClient';
import { ApsConfiguration as IApsConfiguration } from '@aps_sdk/autodesk-sdkmanager/dist/src/iApsConfiguration';
import { Logger } from '@aps_sdk/autodesk-sdkmanager/dist/src/logger';
import { IBuilder } from '@aps_sdk/autodesk-sdkmanager';
export interface Builder {
    addResiliencyConfiguration(resiliencyConfiguration: IResiliencyConfiguration): IBuilder;
    addAuthClient(authClient: IAuthClient): IBuilder;
    addApsConfiguration(apsConfiguration: IApsConfiguration): IBuilder;
    addLogger(logger: Logger): IBuilder;
}
